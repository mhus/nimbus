{
  "name": "@voxel-02/monorepo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm -r run build",
    "build:deps": "pnpm --filter @voxel-02/core run build && pnpm --filter @voxel-02/protocol run build",
    "dev:client": "pnpm --filter @voxel-02/client run dev",
    "dev:server": "pnpm run build:deps && pnpm --filter @voxel-02/server run dev",
    "clean": "rm -rf packages/*/dist packages/*/node_modules node_modules"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
